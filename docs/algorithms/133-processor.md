# ABT-1.3.3-Processor: 텍스트 콘텐츠의 명도 대비 알고리즘 v1.0

### 🔗 WCAG 2.2 Reference
- **SC 1.4.3 Contrast (Minimum) (Level AA)**: The visual presentation of text and images of text has a contrast ratio of at least 4.5:1 (3:1 for large text).
- **SC 1.4.11 Non-text Contrast (Level AA)**: Visual information required to identify UI components and graphical objects has a contrast ratio of at least 3:1 against adjacent colors.

---

이 알고리즘은 지침 1.4.2(명암비)를 준수하며, 텍스트와 배경 간의 대비, 그리고 중요한 비텍스트 요소의 대비가 충분한지 진단한다.

## 1. 데이터 수집 단계 (Data Collection)
- **Text Contrast:** 글자색(`color`), 배경색(`background-color`), 폰트 크기(`font-size`), 굵기(`font-weight`).
- **UI Contrast:** 버튼 테두리, 상태 표시 아이콘, 입력창 경계선 등의 색상.
- **Environment:** 불투명도(`opacity`), 그라데이션, 배경 이미지 유무.

## 2. 분석 및 분류 로직 (Analysis Pipeline)

### [단계 A] 텍스트 명암비 검사 (WCAG 2.2 - 1.4.3)
- **Rule 1.1 (일반 텍스트):** 18pt(24px) 미만 또는 굵은 14pt(18.7px) 미만 텍스트.
  - 대비 < 4.5:1 → **[오류]**
- **Rule 1.2 (큰 텍스트):** 18pt(24px) 이상 또는 굵은 14pt(18.7px) 이상 텍스트.
  - 대비 < 3:1 → **[오류]**

### [단계 B] 비텍스트 명암비 검사 (WCAG 2.2 - 1.4.11)
- **Rule 2.1 (UI 컴포넌트):** 버튼, 입력창 등 컨트롤의 테두리나 상태 구분선.
  - 인접 색상과의 대비 < 3:1 → **[수정 권고]** (KWCAG는 주로 텍스트 위주이나 WCAG AA 기준 적용)

### [단계 C] 특수 상황 처리
- **Rule 3.1:** 배경이 이미지나 그라데이션인 경우.
  - 결과: **[검토 필요]** (평균 대비가 아닌 최저 대비 지점 확인 필요)

## 3. 최종 상태 정의 (Final Status)
1. **오류:** 표준 명암비 미달 (텍스트).
2. **부적절:** 텍스트 명암비는 충족하나 UI 컴포넌트 인지도가 현저히 낮은 경우.
3. **수정 권고:** 배경색과 유사하여 장시간 이용 시 피로감을 주는 경우.
4. **검토 필요:** 이미지 배경 위 텍스트, 반투명 요소.
5. **적절:** 모든 중요 요소가 충분한 대비를 가짐.
