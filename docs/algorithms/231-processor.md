# ABT-6.3.1-Processor: 깜빡임과 번쩍임 사용 제한 알고리즘 v1.0

### 🔗 WCAG 2.2 Reference
- **SC 2.3.1 Three Flashes or Below Threshold (Level A)**: Web pages do not contain anything that flashes more than three times in any one second period, or the flash is below the general flash and red flash thresholds.

---

이 알고리즘은 **KWCAG 6.3.1(깜빡임과 번쩍임 제한)** 지침을 준수하며, 광과민성 발작을 유발할 수 있는 콘텐츠 존재 여부를 전문가에게 안내한다. 번쩍임의 빈도나 강도를 기계적으로 정밀 측정하는 것은 오탐률이 높으므로, **"잠재적 위험 요소 탐지 시 전문가가 직접 확인하도록 가이드하는 것"**을 원칙으로 한다.

## 1. 데이터 수집 단계 (Data Collection)
- **Animation Candidate:** CSS `animation`, `transition` 속성이 포함된 모든 요소.
- **Dynamic Elements:** `<video>`, `<img>` (GIF/APNG), `<canvas>`, `<svg>` 내 애니메이션 요소.
- **Scripted Movement:** `setInterval`, `requestAnimationFrame` 등을 통해 스타일이 동적으로 변하는 요소 추정.

## 2. 분석 및 분류 로직 (Analysis Pipeline)

### [단계 A] 잠재적 깜빡임 요소 추출 (Potential Flashing Detection)
- **Rule 1.1 (애니메이션 감지):** 
  - `infinite` 루프를 가진 CSS 애니메이션이 설정된 요소.
  - 결과: **[검토 필요]**.
  - 가이드: "무한 반복되는 애니메이션이 탐지되었습니다. 초당 3회 이상의 과도한 깜빡임이 발생하는지 전문가가 직접 확인하십시오."

### [단계 B] 멀티미디어 및 동적 리소스 (Multimedia)
- **Rule 2.1 (자동 재생 미디어):** 
  - 자동 재생(`autoplay`)되는 비디오나 움직이는 이미지(GIF 등).
  - 결과: **[검토 필요]**.
  - 가이드: "동적 멀티미디어 콘텐츠가 존재합니다. 내부 콘텐츠에 번쩍이는 섬광이 포함되어 있는지 확인이 필요합니다."

### [단계 C] 전문가 확인 가이드 (Expert Guidance)
- **Rule 3.1 (제어 수단 연계):**
  - 깜빡임 요소 발견 시, 2.2.2 지침에 따른 '정지' 수단이 제공되는지 함께 점검하도록 안내.

## 3. 최종 상태 정의 (Final Status)
1. **검토 필요:** 애니메이션, 비디오, GIF 등 시각적 변화가 있는 모든 요소.
2. **적절:** 페이지 내에 움직이거나 번쩍이는 요소가 전혀 없는 경우.
