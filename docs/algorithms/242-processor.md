# ABT-2.4.2-Processor: 제목 제공 v0.11

### 🔗 References
- KWCAG 2.2: 2.4.2 제목 제공
- WCAG 2.2: 2.4.2 Page Titled (A) (Reference)

---

이 알고리즘은 **KWCAG 2.4.2(제목 제공)** 지침을 준수하며, 페이지와 프레임에 해당 내용을 요약한 제목이 제공되는지 진단한다. 자동화 도구는 제목의 '존재 여부'는 판단할 수 있으나, 그 '내용의 적절성'은 최종적으로 전문가의 검토가 필요하다는 원칙을 따른다.

## 1. 데이터 수집 단계 (Data Collection)
- **Page Title:** `document.title` 값 추출.
- **Frame Titles:** 모든 `<iframe>`, `<frame>` 요소의 `title` 속성 존재 여부 및 값 추출.
- **Context:** 현재 문서의 URL 및 프레임의 위치 정보.

## 2. 분석 및 분류 로직 (Analysis Pipeline)

### [단계 A] 페이지 제목 검사 (Page Title)
- **Rule 1.1 (존재 여부):** `<head>` 내부에 `<title>` 태그가 부재하거나 비어있는 경우 **[오류]**.
- **Rule 1.2 (무의미한 제목):** "Untitled", "index.html", "새 탭", "제목 없음" 등 기본값인 경우 **[부적절]**.
- **Rule 1.3 (내용 검토):** 제목이 존재하더라도 페이지의 목적을 핵심적으로 설명하는지 확인하기 위해 **[검토 필요]** 상태로 리포트 생성.

### [단계 B] 프레임 제목 검사 (Frame Title)
- **Rule 2.1 (속성 누락):** `<iframe>`에 `title` 속성이 아예 없는 경우 **[오류]**.
- **Rule 2.2 (무의미한 제목):** 제목이 "iframe", "프레임", "내용" 등 용도를 알 수 없는 경우 **[부적절]**.
- **Rule 2.3 (내용 검토):** 제목이 존재하더라도 프레임의 용도나 목적을 적절히 설명하고 있는지 확인하기 위해 **[검토 필요]** 상태로 리포트 생성.

### [단계 C] 핵심 주제 제공 검사 (Main Heading)
- **Rule 3.1 (H1 존재 여부):** 페이지 내에 문서의 핵심 주제를 나타내는 `<h1>` 요소가 존재하는가?
  - 결과: 부재 시 **[검토 필요]** (구조적 대주제 제공 및 식별 가능성 확인 유도).

## 3. 최종 상태 정의 (Final Status)
1. **오류:** 페이지 또는 프레임 제목 누락.
2. **부적절:** 의미 없는 제목(기본값), 프레임 용도 불명.
3. **검토 필요:** 제목 문구가 실제 페이지/프레임 내용을 대표하는지 전문가 확인이 필요한 경우.
4. **적절:** (전문가가 '검토 필요' 항목을 확인 후 '적절'로 판정한 경우에만 적용)
