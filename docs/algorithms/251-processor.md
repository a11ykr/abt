# ABT-2.5.1-Processor: 단일 포인터 입력 지원 v0.10

### 🔗 References
- KWCAG 2.2: 2.5.1 단일 포인터 입력 지원
- WCAG 2.2: 2.5.1 Pointer Gestures (A) (Reference)

---

이 알고리즘은 **KWCAG 2.5.1(단일 포인터 입력 지원)** 지침을 준수하며, 복잡한 제스처(스와이프, 드래그, 핀치 줌 등)가 필요한 기능에 대해 단일 포인터(탭/클릭)만으로 조작 가능한 대체 수단이 제공되는지 진단한다.

## 1. 데이터 수집 단계 (Data Collection)
- **Event Listeners:** `touchmove`, `pointermove`, `drag`, `dragstart`, `wheel` 등의 이벤트 리스너가 등록된 요소.
- **Library Signatures:** `slick-slider`, `swiper`, `owl-carousel`, `google-map` 등 제스처 기반 라이브러리 관련 클래스 및 속성.
- **Layout Characteristics:** `overflow-x: scroll`, `touch-action: none` 등 제스처 조작이 예상되는 스타일 속성.
- **Proximity Controls:** 제스처 의심 요소 인근(부모/형제)의 클릭 가능한 요소(`button`, `a`, `[role="button"]`).

## 2. 분석 및 분류 로직 (Analysis Pipeline)

### [단계 A] 제스처 의심 영역 식별 (Heuristic Detection)
- **Rule 1.1 (이벤트 기반):** 이동 및 경로 기반 이벤트 리스너가 감지된 경우.
  - 결과: **[검토 필요]**.
  - 메시지: "이동/경로 기반 이벤트가 감지되었습니다. 단일 포인터(클릭) 대체 수단이 있는지 확인하십시오."
- **Rule 1.2 (컴포넌트 패턴):** 캐러셀이나 지도와 같이 제스처가 필수적인 컴포넌트 구조 탐지.
  - 결과: **[검토 필요]**.

### [단계 B] 대체 조작 수단 탐지 (Alternative Analysis)
- **Rule 2.1 (인접 버튼 검색):** 제스처 영역 주변에 이전/다음, 확대/축소 등을 수행할 수 있는 버튼이 존재하는가?
  - 결과: 버튼 발견 시 **[검토 필요]** (메시지: "주변 버튼이 해당 제스처 기능을 대체하는지 확인하십시오.")
  - 결과: 버튼 미발견 시 **[부적절]** 경고 (메시지: "제스처 필수 영역으로 추정되나 인근에 대체 버튼이 발견되지 않습니다.")

### [단계 C] 기능적 대체 확인 (Manual Review Trigger)
- 본 지침은 프로그램적 의도 파악이 어려우므로, 탐지된 모든 영역에 대해 전문가가 실제 조작을 통해 단일 포인터 작동 여부를 확인하도록 유도한다.

## 3. 최종 상태 정의 (Final Status)
1. **오류:** (자동 판정 지양) 명백한 대체 수단 부재가 코드상 확정될 때.
2. **부적절:** 제스처 영역 주변에 대체 버튼이 전혀 없고, 키보드 접근성도 보장되지 않는 경우.
3. **검토 필요:** (기본 상태) 제스처가 의심되는 모든 영역. 전문가의 실조작 테스트 필요.
4. **적절:** 제스처 영역과 함께 명확하게 식별 가능한 대체 버튼이 존재함이 확인된 경우.
