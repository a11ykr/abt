# ABT-2.5.2-Processor: 포인터 입력 취소 v0.11

### 🔗 References
- KWCAG 2.2: 2.5.2 포인터 입력 취소
- WCAG 2.2: 2.5.2 Pointer Cancellation (A) (Reference)

---

이 알고리즘은 **KWCAG 2.5.2(포인터 입력 취소)** 지침을 준수하며, 마우스 클릭이나 터치 시 실수를 방지하기 위해 기능의 실행 시점이 적절한지 진단한다. 브라우저 확장 프로그램의 기술적 한계(Isolated World)로 인해 `addEventListener`로 등록된 동적 리스너를 완벽히 탐지할 수는 없으며, 인라인 속성 탐지 및 전문가의 수동 동작 검증 가이드를 제공하는 것을 목적으로 한다.

## 1. 데이터 수집 단계 (Data Collection)
- **Inline Event Handlers:** `onmousedown`, `ontouchstart`, `onpointerdown` 등 '누름(Down)' 관련 인라인 속성이 등록된 요소 탐색.
- **Complementary Handlers:** 해당 요소에 `onmouseup`, `ontouchend`, `onclick` 등의 '뗌(Up/Click)' 이벤트가 동반되는지 확인.

## 2. 분석 및 분류 로직 (Analysis Pipeline)

### [단계 A] 인라인 Down-Event 탐지 (Inline Timing Detection)
- **Rule 1.1:** 인터랙티브 요소가 `click`이나 `mouseup` 없이 오직 `mousedown` 또는 `touchstart` 속성만 가지고 있는가?
  - 결과: 감지 시 **[검토 필요]**.
  - 가이드: "이 요소는 누르는 순간 기능이 실행될 가능성이 높습니다. 사용자가 클릭을 완료하기 전(포인터를 요소 밖으로 이동)에 동작을 취소할 수 있는지 확인하십시오."

### [단계 B] 전문가 동작 검증 가이드 (Manual Operation)
- 자동화 도구가 탐지하지 못한 JS 기반 리스너가 있을 수 있으므로, 페이지 내 주요 컨트롤(삭제, 전송, 이동 등)에 대해 전문가가 직접 다음을 테스트하도록 안내한다:
  1. 요소를 마우스로 누른다.
  2. 누른 상태를 유지하며 마우스를 요소 영역 밖으로 옮긴다.
  3. 마우스 버튼을 뗀다.
  4. **결과**: 기능이 실행되었다면 지침 위반, 실행되지 않았다면 준수.

## 3. 최종 상태 정의 (Final Status)
1. **검토 필요:** (기본 상태) 인라인 Down-Event가 발견된 모든 요소. 전문가가 실제 취소 가능 여부를 최종 판정함.
2. **적절:** 표준 `click` 이벤트를 사용하여 '누른 후 밖으로 옮겨 떼기'를 통한 취소가 보장됨이 확인된 경우.
3. **부적절:** 누르는 즉시 파괴적 동작(삭제, 이동 등)이 발생하고 취소 수단이 없음이 확인된 경우 (전문가 판정에 따름).
