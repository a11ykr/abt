# ABT-6.5.2-Processor: 포인터 입력 취소 알고리즘 v1.0

### 🔗 WCAG 2.2 Reference
- **SC 2.5.2 Pointer Cancellation (Level A)**: For functionality that can be operated using a single pointer, at least one of the following is true: No Down-Event, Abort or Undo, Up-Reversal, Essential.

---

이 알고리즘은 **KWCAG 6.5.2(포인터 입력 취소)** 지침을 준수하며, 마우스 클릭이나 터치 시 실수를 방지하기 위해 기능의 실행 시점이 적절한지 진단한다. 시스템은 '실행 시점'의 기술적 부적합성을 탐지하고, '동작의 영향도'는 전문가가 판단하도록 유도한다.

## 1. 데이터 수집 단계 (Data Collection)
- **Event Listener Analysis:** `mousedown`, `touchstart`, `pointerdown` 등 '누름(Down)' 이벤트가 등록된 인터랙티브 요소 추출.
- **Action Monitoring (Heuristic):** Down-Event 내에서 실행되는 주요 함수(경로 이동, 폼 제출, 상태 변경 등)의 존재 여부 확인.
- **Element Context:** 버튼, 링크, 드롭다운 등 사용자 입력에 따라 중요한 변화를 일으키는 요소군.

## 2. 분석 및 분류 로직 (Analysis Pipeline)

### [단계 A] 실행 타이밍 분석 (Timing Detection)
- **Rule 1.1 (Down-Event 기반 실행 탐지):**
  - 인터랙티브 요소가 `click`이 아닌 `mousedown` 등의 Down-Event에 직접적인 기능을 바인딩하고 있는가?
  - 결과: 감지 시 **[검토 필요]**.
  - 메시지: "이 요소는 누르는 즉시(Down-Event) 기능이 실행될 가능성이 있습니다. 포인터를 밖으로 옮겨 취소할 수 있는지 확인하십시오."

### [단계 B] 동작의 영향도 추정 (Impact Heuristics)
- **Rule 2.1 (파괴적/주요 동작 의심):**
  - 함수명이나 속성에 `delete`, `remove`, `pay`, `submit`, `go`, `move` 등 상태 변화를 암시하는 키워드가 포함되어 있는가?
  - 결과: 포함 시 **[검토 필요]** 등급 유지 및 강조.
  - 전문가 질문: "누르는 순간 실행되는 이 동작이 데이터 삭제나 페이지 이동 등 사용자에게 중요한 변화를 일으킵니까?"

### [단계 C] 취소 메커니즘 확인 (Abort/Undo Check)
- **Rule 3.1 (표준 인터랙션 준수 여부):**
  - 브라우저 표준 `click` 이벤트를 사용하여 '누른 후 밖으로 옮겨 떼기'를 통한 취소가 자연스럽게 보장되는가?
  - 결과: 표준 `click` 사용 시 **[적절]**.

## 3. 최종 상태 정의 (Final Status)
1. **부적절:** Down-Event 리스너가 감지된 인터랙티브 요소에 대해 전문가가 수동 검사한 결과, `mousedown` 시점에 페이지 이동이나 데이터 변경과 같은 파괴적 동작이 발생하고 취소할 수 없음이 확인될 때.
2. **검토 필요:** (기본 상태) Down-Event 리스너가 감지된 모든 인터랙티브 요소. ABT 엔진은 Down-Event가 감지된 요소를 목록화하고, 전문가에게 다음을 확인하도록 안내한다:
   - 실제로 Down-Event 시점에 기능이 실행되는가?
   - 실행된다면, 파괴적인 동작(예: 데이터 삭제, 페이지 이동)을 유발하는가?
   - 포인터를 떼기 전(Up-Event)에 동작을 취소할 수 있는가?
3. **적절:** 표준 `click` 이벤트를 사용하거나, 누른 후 뗄 때(Up-Event) 기능이 실행됨이 확인된 경우.
