# ABT Desktop 초기 구축 및 환경 설정 계획

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Electron과 React를 결합한 크로스플랫폼 데스크탑 앱 환경 구축 및 기초 레이아웃 생성

**Architecture:** Electron (Main Process) + React/Vite (Renderer Process) + Tailwind CSS (Styling)

**Tech Stack:** Electron, Vite, React, Tailwind CSS, Lucide React (Icons)

---

### Task 1: 프로젝트 초기화 및 의존성 설치

**Files:**
- Create: `package.json`
- Create: `vite.config.ts`

**Step 1: package.json 생성 및 의존성 정의**
- `electron`, `vite`, `@vitejs/plugin-react` 설치
- `concurrently`, `wait-on` 설치 (개발 모드 실행용)

**Step 2: Vite 설정**
- Renderer 프로세스를 위한 Vite 설정 (React 지원)

**Step 3: Commit**
```bash
git add package.json vite.config.ts
git commit -m "chore: initialize project with electron and react"
```

---

### Task 2: Electron Main 프로세스 구축

**Files:**
- Create: `src/main/main.ts`
- Create: `src/main/preload.ts`

**Step 1: Main 프로세스 작성**
- 브라우저 창 생성 및 개발 모드 시 Vite 개발 서버 연결 로직

**Step 2: Preload 스크립트 작성**
- 렌더러와 메인 프로세스 간 통신을 위한 기초 Bridge 설정

**Step 3: Commit**
```bash
git add src/main/
git commit -m "feat: setup basic electron main and preload scripts"
```

---

### Task 3: Tailwind CSS 및 기초 UI 프레임워크 설정

**Files:**
- Create: `tailwind.config.js`
- Create: `src/renderer/index.css`
- Create: `src/renderer/App.tsx`

**Step 1: Tailwind CSS 초기화**
- `npx tailwindcss init -p` 실행 및 설정 적용

**Step 2: 기초 대시보드 레이아웃 작성**
- 3단 레이아웃 (네비게이터 / 메인 보드 / 속성 패널) 구조 잡기

**Step 3: Commit**
```bash
git add tailwind.config.js src/renderer/
git commit -m "feat: setup tailwind css and initial dashboard layout"
```

---

### Task 4: ABT-111-Processor 데이터 수신 테스트

**Files:**
- Modify: `src/main/main.ts`
- Modify: `src/renderer/App.tsx`

**Step 1: IPC 통신 채널 개설**
- 확장에서 보낼 데이터를 시뮬레이션으로 수신할 수 있는 IPC 채널 구축

**Step 2: 수신 데이터 화면 표시**
- 앞서 작성한 엔진의 JSON 데이터를 화면에 렌더링하는 기초 카드 컴포넌트 작성

**Step 3: Commit**
```bash
git add .
git commit -m "feat: implement basic IPC communication for diagnostic data"
```
